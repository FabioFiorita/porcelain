---
name: product
metadata:
  internal: true
description: What Porcelain is, who it's for, its core features and product principles. Read when designing features, UI, or prioritizing work.
---

# Porcelain — product

A lightweight **hub for agentic coding** (macOS + Linux apps, plus a browser viewer served by the daemon), not an editor: run your coding agents and review their work in one window. (Retires the older "viewer + agent companion, agents live in the terminal" framing — the agents now run *inside* Porcelain, alongside the review surfaces that fill the gaps which used to force Cursor/Zed/GitHub Desktop.)

## Core features

- **File viewer** — fast, no extensions. Text files are *always editable* in place (plain textarea over a Shiki backdrop, debounced autosave — no edit button, no mode toggle), but nothing editor-like: no autocomplete, no multi-file refactors, no format-on-save. Markdown gets a Reader/Source toggle.
- **Split view** — view two files side by side. Deliberately just two panes (no recursive grids) — enough to compare or read-while-editing without becoming an IDE.
- **Scoped navigation** — works in huge monorepos (~50 GB); folders can be hidden/pinned so only relevant apps are visible. Core differentiator: no existing tool lets you hide irrelevant parts of a monorepo.
- **Git** — diffs, worktrees, history, per-file staging, and an in-app commit composer (quick commands + suggestions, no terminal).
- **Flow-ordered review** — review a diff as a *timeline of connected layers*, not an alphabetical file list. A feature change is a straight line (e.g. component → query call → route → controller → service → module → Prisma); Porcelain orders/groups changed files along that dependency flow so the reviewer reads the change as a story from entry point to database. Core differentiator alongside folder hiding.
- **The Review (feature view)** — review the *whole feature* as one agent-authored canvas, not just your diff. When you've only touched part of a feature (e.g. the client half) the diff can't show the rest, because the other files didn't change and the client→server seam isn't an import edge. Your coding agent — which built the feature and knows its boundary — publishes **the Review**: a one-paragraph thesis, then flow-ordered walkthrough sections (entry point → data), each with prose, an optional inline-SVG diagram or sandboxed HTML embed, and the exact code it explains shown inline (diff hunks for changed files, symbol/line slices for the unchanged `shipped`/`context` files it pulls across the seam), plus **loop evidence** as proof the agent closed the loop. It lives in its own **Feature** sidebar tab (Cmd+4): the sidebar is the **outline** (section titles + per-file reviewed marks + progress + Loop evidence row; title click opens the canvas with a clear open affordance); the viewer is a **tabbed canvas** — **Overview** (walkthrough, no buried evidence) and **Loop evidence** (full-height proof when present). File rows open the file with agent-changed lines highlighted (diff via context menu). J/K step Overview chapters; Z is zen (chrome-collapsed). There is **no automatic baseline** — without a published review set the tab shows a "No review yet" empty state (with a copyable prompt for the agent). The agent feeds it through the bundled porcelain CLI (`src/cli/`, `~/.porcelain/porcelain`, the `feature-review` skill). The companion skills ship via **skills.sh** (`npx skills add FabioFiorita/porcelain`); the CLI itself installs automatically and stays fresh on every launch — no per-agent setup. The **agent channel** opens no network port (the CLI only touches local files; the daemon's token-gated loopback/tailnet listener is a separate, documented surface — see the `audit` skill). (The old separate *feature artifact* explainer folded into the walkthrough sections; loop evidence is ephemeral — **Feature Clear removes the review set and the loop-evidence directory together** so nothing is orphaned on disk; the Loop evidence chapter still has its own evidence-only Clear. A later Excalidraw medium for freeform boards is planned; default body remains structured document + HTML evidence.)
- **Explore a feature's flow** — comprehension, not just review: point Porcelain at an existing feature and read its flow read-only, no changes and no agent needed. Seed from a **symbol** (the precise unit — a routing file holds many features, so you seed the route's *handler*, not the file) or coarsely from a whole **file**. Heuristic (no LSP): it follows relative imports, so it won't auto-cross the client→server seam (that's still the agent's `shipped` files). *Find references* shows who calls a symbol; *Explore flow* shows what it uses.
- **Review comments** — annotate a line range or a whole file with a note while reviewing; the notes flow to your coding agent via the porcelain CLI as concrete context, and it can mark each resolved once addressed. Add them from the diff/source right-click ("Add comment" / "Comment on file"); manage them in the Comments Quick Access section. The app→agent counterpart to the feature view's agent→app review set — both keep the human and the agent looking at the same feature.
- **Project board** — a per-repo todo/doing/done kanban (Board sidebar tab + a wide viewer board). Your coding agent reads and updates it via the porcelain CLI (list/create/update/move/delete cards), so it can pick up queued work and reflect progress without you spelling everything out in chat — the planning counterpart to the feature view and review comments.
- **Agent chat** — a per-repo message relay (Chat sidebar tab) so agents (and you) can exchange context across environments — e.g. a Linux remote asks the Mac agent for a Simulator screenshot. A message can also stake a **claim** — the files an agent is working on (`chat post --files/--intent`, retired with `--closes`) — and the Chat tab's **Coordination** panel derives live claims and flags overlaps when two agents touch the same file (advisory, no locking). Porcelain CLI `chat list/post/clear`; not a substitute for the durable project board.
- **Companion surfaces** — per-repo scratchpad (the Notes card, a TipTap WYSIWYG) and pinned files, so the things you care about during a review session stay one glance away. These are *companion* features, not editor features.
- **Embedded terminal + actions** — a real terminal lives in the app (the **Terminal** sidebar tab, Cmd+9), so you can run `claude` next to the review surfaces instead of in a separate Ghostty/Warp window. **Actions** are saved named commands shown in the Terminal tab's Quick Access — one click runs one in a terminal (e.g. a dev server). Actions are a two-way agent channel: your coding agent curates them via the porcelain CLI, but only you *run* one. (This reverses an earlier "companion to the terminal, never a terminal" stance — decided 2026-06-16; the consolidation into one window won out. Git quick-commands/commit still run inline, not through a shell.)
- **Agent threads** — run a coding agent *inside* Porcelain (the **Agent** sidebar tab, Cmd+2). Threads are like terminals — you open several, they persist and keep working while you're elsewhere — but purpose-built: a timeline + composer with model **favorites**, three **permission modes** (approve / auto-edits / full), and image attachments. Three providers behind one surface (Claude Code, Codex, OpenCode), each driven through the user's *installed* CLI. It's **daemon-side**, so the iPad/remote-browser client gets a first-class agent surface — the terminal was the weak spot there (a raw shell on a touch keyboard). Deliberately **conversation-only, doing ONE thing**: review still lives in Changes/Feature, execution in Terminal/Actions — the Agent tab doesn't try to be an IDE, it's where you talk to the agent. A thread can be born into a fresh **worktree** so parallel agents don't collide; the Feature tab's **Review inbox** then surfaces, from any checkout, every other worktree with work awaiting review (changed files, a running thread, or a pushed Review) — one click switches you there. The Session companion (right sidebar) always shows identity for the focused/recent thread (title, provider, status, worktree) even when plan/activity/files are empty.
- **Remote access / environments** — the same client, three ways in: the Mac app on a local daemon, the Mac app pointed at a remote daemon (Settings → Environments), or any browser served by the daemon itself. **Each window picks its own environment** (local project in one window, Linux/Beelink in another) — environments are not app-global. Companion data (actions/commands, notes, board, flow layers, review comments, hidden/pinned folders) is carried Mac ↔ remote by the coding agent via the **`sync-environments`** companion skill (the porcelain CLI + SSH/path remap) — not a Settings seed UI. Two networks reach the browser surface: on your home network the daemon can bind the LAN directly (no Tailscale needed — same Wi-Fi, opt-in, token-gated but cleartext on the LAN), and the tailnet covers away-from-home (WireGuard-encrypted). One token-gated daemon surface either way; PTYs and review state live daemon-side, so they survive reconnects and follow you across devices.

## Principles

- Viewer, not editor: lightweight always wins. Quick single-file edits are in (2026-06-12), but autocomplete, rename, format-on-save, and multi-file editing are still out — the IDE features. Lean on the real editor for those.
- Performance is a feature: must stay fast on a 50 GB monorepo — virtualized lists/trees, lazy fs reads, never index what isn't visible.
- Companion, not competitor: where a best-in-class tool already exists (the editor), Porcelain integrates with it rather than reimplementing it. (The terminal was once on this list; it came in-house when the value of running `claude` in the same window beat staying terminal-free — the editor stays out. Then the **agent itself** came in-house on 2026-07-11: the positioning shifted from "viewer + agent *companion*" to **the hub for agentic coding** — running the agent, not just watching its output, is now core. Same call each time: the surface the whole workflow orbits belongs in the window; the editor still doesn't.)
