# Electron

- Use `pnpm dev`, which also starts its development daemon.
- Follow the [development guide](../../../../docs/development.md) for changes requiring a rebuild
  or restart.
- Validate using the [runtime skill's](../SKILL.md) tool-selection and readiness guidance.
  Use tools that can interact with the native application on the selected host.
