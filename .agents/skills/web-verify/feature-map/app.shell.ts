export default {
  feature: 'app.shell',
  path: '/',
  behavior:
    'An unpaired browser reaches the real server and sees the pairing instructions.',
  spec: 'apps/web/spec/browser/app-shell.browser.ts',
} as const;
