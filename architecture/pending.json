{
  "porcelain/fakes-store": {
    "apps/server/spec/fakes/in-memory-event-publisher.ts": {
      "A fake keeps its state private and readonly; seed it through the constructor and read it back through the port.": 1,
      "A fake stores state, it never records calls; assert through what the port reads back.": 1
    },
    "apps/server/spec/fakes/scripted-inventory-refresh.ts": {
      "A fake keeps its state private and readonly; seed it through the constructor and read it back through the port.": 1,
      "A fake stores state, it never records calls; assert through what the port reads back.": 1
    },
    "apps/server/spec/fakes/scripted-reviewed-marks-invalidation.ts": {
      "A fake keeps its state private and readonly; seed it through the constructor and read it back through the port.": 1
    }
  },
  "porcelain/operation-class-shape": {
    "apps/server/src/use-cases/reviews/read-review-summary.ts": {
      "Use case execute takes (context) or (input, context); the context always comes last.": 1
    }
  },
  "porcelain/port-shape": {
    "apps/server/src/ports/device-connections.ts": {
      "A port input is a named model from models/ or the kernel, never an inline or primitive type.": 2,
      "Name a port for its role: it ends in Store, Reader, Writer, Runner, Source, Publisher, Watcher, Probe or Logger, or it is Clock.": 1
    },
    "apps/server/src/ports/event-publisher.ts": {
      "A port input is a named model from models/ or the kernel, never an inline or primitive type.": 3,
      "A port method takes (), (input) or (input, signal): one input object, then the signal.": 6
    },
    "apps/server/src/ports/web-root-files.ts": {
      "A port input is a named model from models/ or the kernel, never an inline or primitive type.": 3,
      "A port method takes (), (input) or (input, signal): one input object, then the signal.": 3,
      "Name a port for its role: it ends in Store, Reader, Writer, Runner, Source, Publisher, Watcher, Probe or Logger, or it is Clock.": 1
    },
    "apps/server/src/ports/worktree-watcher.ts": {
      "A port input is a named model from models/ or the kernel, never an inline or primitive type.": 2,
      "A port method takes (), (input) or (input, signal): one input object, then the signal.": 4
    }
  }
}
