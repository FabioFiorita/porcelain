/** Re-export shared pure terminal key mapping — implementation in @porcelain/client-runtime. */
export {
  type ArrowDirection,
  controlByte,
  type EditChord,
  type TerminalModifier,
  terminalArrowBytes,
  terminalEditBytes,
  terminalModifierBytes,
} from '@porcelain/client-runtime/terminal-keys'
