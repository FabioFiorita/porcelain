/**
 * Compatibility re-export — Settings lives under features/settings now that it
 * is real product UI rather than shell chrome placeholders.
 */
export {
  DataSettings,
  EnvironmentsSettings,
  GeneralSettings,
  ReviewSettings,
  SettingsScreen,
} from '@/features/settings/settings-screen'
