import { ReviewCommentsScreen } from '@/features/comments'

/** Review conversation for the selected checkout, reached from Changes. */
export default function ChangesCommentsRoute(): React.JSX.Element {
  return <ReviewCommentsScreen />
}
