import { CanvasListScreen } from '@/features/canvas'

/** The Worktree's Canvas surface — its Canvases, and the review comments left on it. */
export default function CanvasRoute(): React.JSX.Element {
  return <CanvasListScreen />
}
