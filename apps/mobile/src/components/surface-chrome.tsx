/**
 * Compatibility name for the shared surface chrome. The implementation lives in
 * `panel-chrome` so every mobile surface uses one set of primitives.
 */
export {
  ActionSheet,
  ConfirmDialog,
  EmptyNote,
  ErrorNote,
  IconAction,
  PanelLabel,
  ScreenHeader,
  type SheetAction,
  StatusNote,
} from './panel-chrome'
