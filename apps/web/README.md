# @porcelain/web

React human UI. Served by the daemon to browsers and loaded by the Electron shell.
See [`docs/architecture.md`](../../docs/architecture.md).

**Build status:** source here; still built by `apps/desktop` electron-vite into
`apps/desktop/out/renderer` until the independent web Vite pipeline lands.
