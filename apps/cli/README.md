# @porcelain/cli

Agent-facing `porcelain` binary. Ships as a Node-builtins-only bundle the daemon
installs into `~/.porcelain`. See [`docs/architecture.md`](../../docs/architecture.md).

**Build status:** source here; still emitted by `apps/desktop` electron-vite as
`out/main/cli/porcelain.js` until the independent CLI build lands.
