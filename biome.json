{
  "$schema": "https://biomejs.dev/schemas/2.5.0/schema.json",
  "vcs": { "enabled": true, "clientKind": "git", "useIgnoreFile": true },
  "files": {
    "includes": [
      "**",
      "!.agents",
      "!apps/mobile/assets",
      "!.porcelain",
      "!.porcelain-dev",
      "!marketing",
      "!apps/web/src/components/ui",
      "!**/*.css"
    ]
  },
  "formatter": {
    "enabled": true,
    "indentStyle": "space",
    "indentWidth": 2,
    "lineWidth": 100
  },
  "javascript": {
    "formatter": { "quoteStyle": "single", "semicolons": "asNeeded" },
    "globals": ["__PORCELAIN_VERSION__"]
  },
  "linter": {
    "enabled": true,
    "rules": {
      "preset": "recommended",
      "suspicious": { "noExplicitAny": "error" },
      "correctness": {
        "noUnusedImports": "error",
        "noUnusedVariables": "error"
      }
    }
  },
  "assist": { "actions": { "source": { "organizeImports": "on" } } },
  "overrides": [
    {
      "includes": ["apps/mobile/src/**"],
      "linter": {
        "rules": {
          "a11y": {
            "useValidAriaRole": { "level": "error", "options": { "ignoreNonDom": true } }
          },
          "nursery": {
            "noInlineStyles": "error"
          }
        }
      }
    },
    {
      "includes": [
        "apps/mobile/src/**",
        "!apps/mobile/src/lib/**",
        "!apps/mobile/src/**/use-*.ts",
        "!apps/mobile/src/**/*-store.ts"
      ],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "@/lib/daemon/queries": "Features reach the daemon through their use-<feature>.ts hooks — never import lib/daemon/queries from a component.",
                  "@/lib/daemon/client": "Features reach the daemon through their use-<feature>.ts hooks — never import lib/daemon/client from a component.",
                  "@/lib/daemon/watch": "Features reach the daemon through their use-<feature>.ts hooks — never import lib/daemon/watch from a component."
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["apps/daemon/src/**", "apps/web/src/**", "packages/shared/src/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "electron": "Electron stays in apps/desktop main/preload — daemon, web client, and packages/shared must remain Electron-free.",
                  "@electron-toolkit/preload": "Electron stays in apps/desktop main/preload — daemon, web client, and packages/shared must remain Electron-free.",
                  "@electron-toolkit/utils": "Electron stays in apps/desktop main/preload — daemon, web client, and packages/shared must remain Electron-free."
                }
              }
            }
          }
        }
      }
    },
    {
      "includes": ["apps/web/src/components/**"],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": {
              "level": "error",
              "options": {
                "paths": {
                  "@renderer/lib/trpc": "Components must reach the server through hooks in @renderer/hooks/* — never import lib/trpc directly.",
                  "../lib/trpc": "Components must reach the server through hooks in @renderer/hooks/* — never import lib/trpc directly.",
                  "../../lib/trpc": "Components must reach the server through hooks in @renderer/hooks/* — never import lib/trpc directly.",
                  "@renderer/lib/daemon": "Components must reach the daemon session through hooks (use-app-events, use-terminal-channel, use-files) — never import lib/daemon directly.",
                  "../lib/daemon": "Components must reach the daemon session through hooks (use-app-events, use-terminal-channel, use-files) — never import lib/daemon directly.",
                  "../../lib/daemon": "Components must reach the daemon session through hooks (use-app-events, use-terminal-channel, use-files) — never import lib/daemon directly.",
                  "electron": "Electron stays in apps/desktop main/preload — daemon and web must remain Electron-free.",
                  "@electron-toolkit/preload": "Electron stays in apps/desktop main/preload — daemon and web must remain Electron-free.",
                  "@electron-toolkit/utils": "Electron stays in apps/desktop main/preload — daemon and web must remain Electron-free."
                }
              }
            }
          }
        }
      }
    }
  ]
}
