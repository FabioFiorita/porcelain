# CON-011 — Move Project Data procedure schemas into the Project Data contract module

- Status: Draft
- Batch: 0 — architecture and contract foundations
- Domain: `project-data`
- Depends on: `CON-010` (Draft)
- Governing decisions: [005](../decisions/005-contracts-own-the-wire.md), [013](../decisions/013-use-ten-canonical-product-domains.md), [015](../decisions/015-make-a-clean-pre-launch-cutover.md)
- Primary exemplar: no

## Objective

Create the final domain-owned procedure record for the six Project Data procedures and reduce the
shrink-only transitional ledger from six entries to zero.

## Why this unit exists

Repo notes and companion dispositions are repository data, not Review, Git, or the generic settings
bucket. They complete the ten canonical product domains before the fallback catalog can be removed.

## Current behavior and evidence

`repoNotes` and `setRepoNotes` are in `apps/daemon/src/router/repos.ts`; `companionDispositions`,
`companionGitVisibility`, `setCompanionGitVisibility`, and `setCompanionDisposition` are in
`apps/daemon/src/router/settings.ts`. The exact procedure inventory is in
[`backend.md`](../inventory/backend.md).

## Scope

Create `packages/contracts/src/project-data/{project-data.contract,project-data.procedures,index}.ts`,
its tests, and remove only these final six entries from `unmigratedProcedureLedger`.

## Non-goals

Do not move routers, alter repository storage, merge this surface into Projects, or delete the
horizontal catalog. `CON-012` performs that cutover.

## Target ownership and public surface

`projectDataProcedures` is a `satisfies Record<ProjectDataProcedureName, ProcedureContract>` record.
`repoNotes`, `companionDispositions`, and `companionGitVisibility` are `query`; `setRepoNotes`,
`setCompanionGitVisibility`, and `setCompanionDisposition` are `mutation`. The contract module owns
the exact notes, visibility, and disposition serializable values plus inferred wire types.

## Behavior to preserve

Preserve nullable/missing notes semantics, current disposition vocabulary, repository path handling,
and current success response shapes.

## Legacy behavior to delete

Delete only the six Project Data entries from the ledger. Keep all `procedures/` files and their
exports until `CON-012` verifies every domain record is complete.

## Ordered implementation

1. Transcribe the current inputs and outputs as strict Project Data schemas.
2. Export the kind-aware procedure record from the domain entry point and root contracts index.
3. Remove the six ledger entries; the ledger must now be empty.

## Tests

Add `packages/contracts/src/project-data/project-data.procedures.test.ts` covering valid and invalid
inputs/outputs for every procedure, plus the empty-ledger assertion.

## Validation and evidence

```text
pnpm --dir apps/desktop test -- ../../packages/contracts/src/project-data/project-data.procedures.test.ts ../../packages/contracts/src/procedure-ledger.test.ts
node scripts/lint-procedure-contracts.mjs
git diff --check
```

## Forbidden shortcuts

Do not use `z.unknown`, an alias named `settings`, or treat a zero ledger as permission to delete
fallback files in this recipe.

## Completion criteria

- [ ] Six exact Project Data records declare the required query/mutation kinds.
- [ ] The ledger is empty and has only shrunk since `CON-001`.
- [ ] Contract tests, lint script, and diff check pass.

## Handoff

`CON-012` consumes all ten domain records and removes the transitional catalog in one cutover.
