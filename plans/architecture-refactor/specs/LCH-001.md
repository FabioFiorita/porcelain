# LCH-001 — Clean-v1 seed/reset and protocol match/mismatch launch rehearsal

- Status: Draft

## Granted authorization (2026-08-11, Fabio Fiorita)

The blocking human decision is recorded in `PDT-004.md` ("Granted authorization"): no
export/backup machinery (owner declined it — his real repos already persist in repo-level
`.porcelain`); named reset targets are `~/.porcelain` and the daemon userData root; the reset is
a documented manual procedure only the owner runs — never an agent or script wired into pnpm.
This unit therefore proceeds on isolated fixtures exactly as scoped below, and its rehearsal
documentation references the manual procedure instead of a backup-verified reset command.
- Batch: 9
- Domain: Agent foundations and launch
- Depends on: DAT-001, REM-004, PDT-001, AGT-002
- Governing decisions: 005, 009, 014, 015
- Primary exemplar: no

## Objective

Rehearse launch from explicit clean version-1 seed data and protocol match/mismatch behavior without touching human material. This unit is blocked until a separate, exact human-data authorization is granted.

## Why this unit exists

Decision 015 permits code to stop reading obsolete pre-launch formats after cutover but does not authorize silent deletion. A clean-v1 proof needs controlled fixtures now; any reset/export of real user data needs explicit scope and approval.

## Current behavior and evidence

- Migration order states no architecture executor deletes real companion data.
- The inventory identifies material roots: user config/access/trust and project `.porcelain` Review/Board/Actions/Files data; historical home records and flat active review remain deletion inputs, not automatic cleanup.
- Target launch requires protocol match/mismatch and empty or explicit v1 seed proof.

## Scope

- Prepare only isolated test fixtures and launch rehearsal commands.
- Do not run a real-data export/reset/delete while blocked.

## Non-goals

- Do not reset `~/.porcelain`, userData, or any checkout `.porcelain`.
- Do not infer user approval from a test request, branch, clean checkout, or legacy-path removal.

## Target ownership and public surface

A clean-v1 fixture declares exact root, schema version, seed values, protocol version, and expected match/mismatch result. A real-data operation requires a separate authorization document naming absolute targets, material/disposable classification, export destination/verification, recovery window, destructive commands, and the human's explicit approval.

## Behavior to preserve

Preserve all real data, corruption backups, atomic writes, auth secrets, and current-format intentional defaults. Mismatch is a clear update-required/public-error result, not fallback parsing.

## Legacy behavior to delete

Delete historical readers/migrations only through their owning domain cutovers; LCH-001 authorizes none of those deletions and no data reset.

## Ordered implementation

1. Create clean temporary userData/home/project fixture constructors with explicit v1 seeds; never resolve a fixture path from a real home/check-out.
2. Add protocol match and mismatch tests against the REM-004 handshake contract; assert mismatch blocks normal client operation and exposes only the update-required public error.
3. Document a real-data authorization template containing every target and recovery field in Target ownership; leave its authorization field blank.
4. Stop this spec at the blocker. Do not add a reset command to scripts or documentation until the authorization is completed and accepted.

## Tests

- Controlled fixture tests prove clean-v1 startup, protocol match, and mismatch.
- `pnpm test`, `pnpm lint`, and the REM-004 contract/integration suite pass.

## Validation and evidence

Blocked evidence is the fixture test output plus a confirmed absence of real-data commands. No real-data evidence is collected while authorization is absent.

## Forbidden shortcuts

- Do not run `rm`, overwrite, migration purge, or reset against a user/config/project root.
- Do not convert a real unversioned document in place during rehearsal.
- Do not mark this Ready/Landed without the exact authorization.

## Completion criteria

Formerly Blocked; the recorded authorization in PDT-004 resolves the blocker. The unit completes
on passing isolated fixture tests (clean-v1 startup, protocol match, mismatch) plus documented
manual reset procedure. Only isolated fixtures may run; real-data commands remain absent.

## Handoff

After authorization, a new or amended approved spec performs the named external operation with backup verification; this blocked foundation does not self-unblock.
