# REV-010 — Prove Review active-content security, containment, resilience, and completed-domain gate

- Status: Draft
- Batch: 5
- Domain: Review
- Depends on: REV-004, REV-009, PDT-006
- Governing decisions: 006, 009, 011, 015
- Primary exemplar: no

## Objective

Prove retained Review security and recovery behavior at its final owners and mechanically mark Review complete only when target contracts, paths, callers, and legacy ledger are clean.

## Why this unit exists

Review handles agent-authored prose/HTML/results/assets and repository paths; clean vocabulary deletion must not discard sandboxing, CSP, containment, caps, malformed input handling, regex resilience, reviewed reconciliation, or watcher recovery.

## Current behavior and evidence

- Audit assigns Review containment, escaped prose, sandboxed active content, CSP, Evidence caps, regex compile-on-read, and reviewed reconciliation.
- Current code includes `fs/evidence-assets.ts`, `review/doc-set.ts`, `stores/{evidence-store,reviewed-store,comment-store,review-store}.ts`, and Web/mobile Evidence rendering.
- Inventory explicitly retains malformed/corrupt external input, bounds, recovery, and checks-only defaults.

## Scope

- Add target-owner threat/failure documentation and focused tests/gates for every retained Review invariant.
- Mark Review migration registry complete and remove its last ledger entries only after proof.

## Non-goals

- Do not reintroduce legacy root index/Feature aliases, relax CSP/containment, or reset real data.
- Do not make generic support/Viewer own Review authored-content policy.

## Target ownership and public surface

Review owns an active-content policy: escaped prose by default; HTML only in sandboxed iframe with explicit CSP; result/asset paths pass Files containment; documents/assets/checks have caps; malformed external content yields explicit safe unavailable/omission states; reviewed marks reconcile against current fingerprints; watcher failure/re-registration has defined recovery.

## Behavior to preserve

Preserve caps, asset/document path checks, safe HTML sandbox/CSP, checks-only packs, oversized unavailable state, corrupt/malformed dispositions, reviewed reconciliation, comment safety, and watcher recovery.

## Legacy behavior to delete

Delete no new compatibility. Remove final Review registry legacy entries only after REV-009 zero-hit proof and every retained invariant has a test/gate.

## Ordered implementation

1. Write `docs/internals/review-security.md` indexing every invariant, owner module, threat/failure, test, and gate.
2. Add tests for escaping, iframe sandbox/CSP, hostile relative/absolute/traversal/symlink assets, caps, malformed meta/docs, oversized Results, regex compile failure, reviewed stale fingerprint pruning, watcher failure/re-registration.
3. Add static lint assertions for target HTML iframe sandbox/CSP attributes and forbidden raw asset path exposure; use runtime tests for behavior.
4. Run real temporary filesystem adapter tests and contract-valid Web/mobile rendering tests; retain browser E2E only for assembled watcher wiring.
5. Confirm all Review target roots/public entries and no direct store/router cross-boundary imports with architecture lint.
6. Change `DOMAIN_MIGRATIONS.review` to `complete` and remove exact Review legacy paths only after all named tests/searches pass.

## Tests

- Focused security/resilience tests at Review, Files, Web/mobile boundaries.
- Architecture/Review cutover lint fixtures; `pnpm test`, `pnpm lint`, `pnpm verify` pass.

## Validation and evidence

Record threat-to-test table, safe rendering evidence, hostile-path/cap/corruption outputs, watcher recovery proof, zero legacy ledger, and registry status transition.

## Forbidden shortcuts

- Do not test security only with a snapshot or mark complete on one happy path.
- Do not expose absolute paths, unsandboxed HTML, raw error messages, or unbounded content.
- Do not change registry status to hide remaining legacy code.

## Completion criteria

Every retained Review invariant has owner/test/gate, target imports/paths are clean, Review legacy ledger is zero, registry says complete, and full gates pass.

## Handoff

LCH-002 and LCH-003 consume Review completion and launch evidence.
