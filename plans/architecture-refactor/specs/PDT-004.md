# PDT-004 — Record human-only reset authorization and classification fixtures

- Status: Landed
- Batch: 4 — Project Data and complete Review
- Domain: project-data
- Depends on: PDT-001, PDT-002, PDT-003, DAT-001
- Governing decisions: [009](../decisions/009-treat-realtime-as-recoverable-signals-and-streams.md), [015](../decisions/015-make-a-clean-pre-launch-cutover.md)
- Primary exemplar: no
- Skills required at execution: `ship`, `execute-architecture-spec`
- Review HEAD: `80088e35` (clean; PDT-003 Landed; 87/110)
- Preflight revision: current-tree review after `80088e35`. Catalog dependencies
  PDT-001, PDT-002, PDT-003, and DAT-001 are Landed. The thin Draft's
  export/backup protocol, unfilled template, "blocked until authorization",
  `pnpm test` whole-suite command, and parenthetical blocker wording are
  superseded by the Granted authorization and the rulings below.

## Granted authorization (2026-08-11, Fabio Fiorita)

The blocking human decision is already recorded. These terms bind the
executor and supersede every leftover export/backup/blocker sentence in
the tracked Draft:

- **No export/backup machinery.** The owner confirmed every real repo he
  uses already persists in repo-level `.porcelain`, he is the only
  pre-launch user, and he declines the export/backup requirement. This
  unit lands the authorization document, classification fixtures, and
  validation only.
- **Named reset targets** (for the eventual clean-v1 launch step the
  owner runs himself): `~/.porcelain`, the daemon userData root
  (`~/.local/share/porcelain`), and — as an optional launch-cutover step
  only — repo-level `.porcelain` directories, which otherwise remain the
  permanent persistence model.
- **Human-only execution.** The reset is a documented manual procedure.
  No agent, orchestrator, CI job, Vitest case, or pnpm script may run it
  or resolve those live roots for mutation.
- **Decision 015 stands.** Post-cutover code stops reading obsolete
  shapes but never silently deletes them. If a real file becomes
  unreadable at launch, recovery is manual conversion of the on-disk
  file, not restoration from an archive.

## Architecture reviewer rulings (bind the executor; supersede the tracked Draft)

1. **Depends-on line is catalog IDs only.** Write
   `Depends on: PDT-001, PDT-002, PDT-003, DAT-001` with no parenthetical
   statuses. All four are Landed at `80088e35`.

2. **Drop every export/backup/restore artifact.** Do not add a manifest
   format, integrity checksum, restore command, encryption policy,
   archive destination, or recovery-window timer. The Draft's
   "missing restore verification" refusal is replaced by refusals for
   `exportMachinery: true` and `agentRunnable: true`. Decision 015
   recovery is `manual-on-disk-conversion`.

3. **Authorization document is filled, not a template.** Write
   `plans/architecture-refactor/authorization/pdt-004-human-reset.md`
   with the exact headings and values under Target ownership. Keep this
   recipe's Granted authorization section as the recorded decision. Do
   not leave an unfilled approval field.

4. **Machine-checkable encoding lives next to DAT-001, not in the
   feature public surface.** Add:

   - `apps/daemon/src/project-data/reset-authorization.ts`
   - `apps/daemon/src/project-data/reset-authorization.test.ts`

   DAT-001 already owns `apps/daemon/src/project-data/strict-json-document.ts`
   as a supporting primitive. Do **not** export the new module from
   `apps/daemon/src/features/project-data/index.ts`. Do **not** add a
   client-runtime, Web, mobile, CLI, router, or contracts symbol. Do
   **not** register a new target root; `apps/daemon/src/project-data/`
   stays a supporting import. Domain status stays `migrating`.

5. **Bind the three granted roots and the file catalog below.** Path
   templates in the authorization record are exactly
   `~/.porcelain`, `~/.local/share/porcelain`, and `<repo>/.porcelain`.
   Fixture trees use `withTemporaryDirectory` from
   `apps/daemon/src/testing/temporary-directory.ts` and never pass a live
   home, userData, or checkout companion path.

6. **Validation is a pure parse of an in-memory record.**
   `parseResetAuthorization` must not read, write, rename, or unlink any
   filesystem path. `classifyResetRecord` is a table lookup on
   `(root, relativePath)`. `isForbiddenLiveRoot` may *compare* against
   `porcelainHome()`, `join(homedir(), '.porcelain')`,
   `join(homedir(), '.porcelain-dev')`,
   `join(homedir(), '.local/share/porcelain')`,
   `join(homedir(), '.local/share/porcelain-dev')`,
   `process.env.PORCELAIN_HOME`, and `process.env.PORCELAIN_USER_DATA`
   so tests can prove those roots are refused; it must not mutate them.

7. **Leave migrations and live data untouched.**
   `apps/daemon/src/project/migrate-home.ts`
   (`ensureProjectCompanion`, `resetProjectCompanionMemo`,
   `hasProjectCompanion`) and
   `apps/daemon/src/project/migrate-active-review.ts` stay on disk.
   PDT-005 deletes them after this authorization. Do not call
   `porcelainHomePath` to build a write target. Do not add a package.json
   script, CLI noun, or `scripts/*.mjs` entry that resets, exports,
   backs up, wipes, or seeds a live porcelain home or userData.

8. **Do not start PDT-005, PDT-006, LCH-001, or any later recipe.**
   Do not mark `project-data` complete.

## Objective

Land the filled human-only reset authorization, isolated classification
fixtures, and validation that refuses a malformed authorization record,
without adding export machinery or any agent-runnable reset.

## Why this unit exists

Decision 015 lets code stop reading obsolete pre-launch formats after
cutover and forbids silent erase of human material. Clean-v1 launch
(PDT-005 / LCH-001) cannot delete `~/.porcelain`, application userData,
or repository `.porcelain` until this recorded authorization and its
classification fixtures exist. Historical `ensureProjectCompanion`
copy/purge is not that authorization.

## Current behavior and evidence

Verified at `80088e35`:

- Production home: `packages/shared/src/porcelain-home.ts`
  `porcelainHome()` → `process.env.PORCELAIN_HOME ?? join(homedir(), '.porcelain')`;
  `porcelainHomePath(...parts)` joins under that root. Dev stack uses
  `~/.porcelain-dev` via `scripts/dev-env.mjs` `PRIMARY_PROFILE.home`.
- Production userData: `scripts/dev-env.mjs` comments and
  `scripts/build-daemon-dist.mjs` systemd unit name
  `~/.local/share/porcelain`. The daemon does not invent this path;
  `apps/daemon/src/server.ts` requires `PORCELAIN_USER_DATA` and calls
  `initConfigDir(userData)` plus `initProjectsRecentsDir(userData)`.
  Dev stack uses `~/.local/share/porcelain-dev`.
- userData files: `apps/daemon/src/features/remote/remote-config-store.ts`
  writes `join(configDir, 'config.json')` (`tailnetBind` / `lanBind` /
  `funnelBind`); `apps/daemon/src/features/projects/projects-recents-store.ts`
  writes `join(directory, 'projects-recents.json')`.
- Home secrets: `apps/daemon/src/features/remote/access-store.ts`
  `process.env.PORCELAIN_ACCESS_FILE ?? porcelainHomePath('access.json')`;
  `apps/daemon/src/net/admin-token.ts` `admin-token`;
  `apps/daemon/src/features/actions/json-action-trust-store.ts`
  `process.env.PORCELAIN_ACTION_TRUST_FILE ?? porcelainHomePath('action-trust.json')`.
- Historical home channels still listed in
  `apps/daemon/src/project/migrate-home.ts` `homeChannels`:
  `actions.json`, `board.json`, `layers.json`, `scope.json`,
  `notes.json`, `comments.json`, `reviewed.json`, `review-sets.json`,
  `feature-view.json`. `loop-evidence` remains the home evidence
  directory that migrator copies. Exported symbols:
  `ensureProjectCompanion`, `resetProjectCompanionMemo`,
  `hasProjectCompanion`.
- Repo companion catalog: `packages/shared/src/project-porcelain.ts`
  `PROJECT_FILES`, `ACTIVE_FILES`, `PROJECT_ACTIVE_DIR`,
  `PROJECT_REVIEWS_DIR`, `PROJECT_EVIDENCE_DIR`, `ALWAYS_IGNORED`.
  v1 root marker is `PROJECT_FILES.manifest` =
  `project-manifest.json` (PDT-001). Notes are `notes.md` (PDT-002).
- No authorization file exists under `plans/architecture-refactor/`
  besides this recipe. Root `package.json` has no reset/export/backup/
  wipe script. `apps/daemon/src/project-data/` contains only
  `strict-json-document.ts` and its test.
- Inventory classification (planning evidence, not a second schema):
  `plans/architecture-refactor/inventory/data-and-workflows.md`
  persisted-roots table. Decision 015
  `plans/architecture-refactor/decisions/015-make-a-clean-pre-launch-cutover.md`
  "Existing data and destructive scope" plus rejected alternative
  "Silently reset incompatible data at startup."
- Isolated-temp idiom: `withTemporaryDirectory` in
  `apps/daemon/src/testing/temporary-directory.ts` (PDT-001 tests).

## Scope

1. Write the filled authorization document at
   `plans/architecture-refactor/authorization/pdt-004-human-reset.md`.
2. Add `reset-authorization.ts` + `.test.ts` under
   `apps/daemon/src/project-data/` with the exact public surface below.
3. Flip this recipe and the catalog row to **Landed** only in the
   implementation commit.

Every new production file is ≤ 450 lines.

## Non-goals

- Export, backup, restore, integrity-manifest, or encryption machinery.
- An agent-invocable script, CLI verb, or pnpm command that resolves or
  deletes `~/.porcelain`, `~/.local/share/porcelain`,
  `~/.porcelain-dev`, `~/.local/share/porcelain-dev`, or a checkout
  `.porcelain`.
- Deleting `migrate-home.ts` / `migrate-active-review.ts` (PDT-005).
- Marking `project-data` complete (PDT-006).
- Launch rehearsal / protocol match (LCH-001).
- Changing Remote access/config stores, Actions trust, Projects recents
  payloads, or any domain adapter schema.
- Starting any later recipe. Pushing.

## Target ownership and public surface

### Filled authorization document

Path: `plans/architecture-refactor/authorization/pdt-004-human-reset.md`

Required headings, in order, with these exact values:

```markdown
# PDT-004 human-only production reset

## Approval
- Approved: yes
- Approved by: Fabio Fiorita
- Approved on: 2026-08-11
- Operator: human-owner-only
- Agent runnable: no
- pnpm wired: no
- Export machinery: no
- Recovery: manual-on-disk-conversion

## Named targets
| Kind | Path template | Included by default | Root disposition |
| --- | --- | --- | --- |
| home | ~/.porcelain | yes | mixed |
| userData | ~/.local/share/porcelain | yes | material |
| repoCompanion | <repo>/.porcelain | no | material |

## Manual procedure
```

The Manual procedure section must tell the owner, in prose, to:

1. Stop the production daemon on port 43117.
2. Remove only `~/.porcelain` and `~/.local/share/porcelain` if they
   exist, accepting irreversible loss of every classified record under
   those roots (no archive exists).
3. Remove a checkout `<repo>/.porcelain` only when that owner decides a
   launch cutover requires a blank companion; otherwise leave it as the
   permanent persistence model.
4. Never point the procedure at `~/.porcelain-dev` or
   `~/.local/share/porcelain-dev`.
5. Never ask an agent, CI job, or `pnpm` script to perform those
   removals.

Do not include shell one-liners that an agent could copy-run as a
wired command (no `rm -rf ~/.porcelain` fenced command). Prose is the
procedure.

### Supporting module

`apps/daemon/src/project-data/reset-authorization.ts` exports exactly:

```ts
export type ResetTargetKind = 'home' | 'userData' | 'repoCompanion'

export type ResetRootDisposition = 'material' | 'disposable' | 'mixed'

export type ResetRecordDisposition = 'material' | 'disposable'

export type ResetTargetSpec = {
  readonly kind: ResetTargetKind
  readonly pathTemplate: string
  readonly includedByDefault: boolean
  readonly rootDisposition: ResetRootDisposition
}

export type ResetAuthorizationRecord = {
  readonly id: 'PDT-004'
  readonly approved: true
  readonly approvedBy: string
  readonly approvedOn: string
  readonly exportMachinery: false
  readonly agentRunnable: false
  readonly pnpmWired: false
  readonly operator: 'human-owner-only'
  readonly recovery: 'manual-on-disk-conversion'
  readonly targets: readonly ResetTargetSpec[]
}

export type ResetAuthorizationRefusal =
  | 'relative-path'
  | 'duplicate-target'
  | 'missing-classification'
  | 'absent-approval'
  | 'export-machinery-present'
  | 'agent-runnable'
  | 'unknown-target'
  | 'invalid-shape'

export const GRANTED_RESET_TARGETS: readonly ResetTargetSpec[] = [
  {
    kind: 'home',
    pathTemplate: '~/.porcelain',
    includedByDefault: true,
    rootDisposition: 'mixed',
  },
  {
    kind: 'userData',
    pathTemplate: '~/.local/share/porcelain',
    includedByDefault: true,
    rootDisposition: 'material',
  },
  {
    kind: 'repoCompanion',
    pathTemplate: '<repo>/.porcelain',
    includedByDefault: false,
    rootDisposition: 'material',
  },
]

export const GRANTED_RESET_AUTHORIZATION: ResetAuthorizationRecord

export function parseResetAuthorization(
  input: unknown,
):
  | { readonly ok: true; readonly value: ResetAuthorizationRecord }
  | { readonly ok: false; readonly reason: ResetAuthorizationRefusal }

export function classifyResetRecord(input: {
  readonly root: ResetTargetKind
  readonly relativePath: string
}): ResetRecordDisposition | null

export function isForbiddenLiveRoot(absolutePath: string): boolean
```

`GRANTED_RESET_AUTHORIZATION` is the granted record: `id: 'PDT-004'`,
`approved: true`, `approvedBy: 'Fabio Fiorita'`,
`approvedOn: '2026-08-11'`, the three false/no machinery flags,
`operator: 'human-owner-only'`, `recovery: 'manual-on-disk-conversion'`,
`targets: GRANTED_RESET_TARGETS`.

`parseResetAuthorization` uses Zod `.strict()` on the record and on
each target. It returns the mapped refusal and never throws on bad
input:

| Input | `reason` |
| --- | --- |
| any `pathTemplate` that is not exactly one of the three granted templates and is not an absolute path (`path.isAbsolute`) | `relative-path` |
| two targets with the same `kind` or the same `pathTemplate` | `duplicate-target` |
| a target missing `rootDisposition`, or `rootDisposition` not in the union | `missing-classification` |
| `approved` missing or not `true` | `absent-approval` |
| `exportMachinery` missing or not `false` | `export-machinery-present` |
| `agentRunnable` or `pnpmWired` missing or not `false` | `agent-runnable` |
| a `kind` / `pathTemplate` pair that is not in `GRANTED_RESET_TARGETS` when `pathTemplate` is a granted template (fixture absolute paths are allowed; granted templates must match their kind) | `unknown-target` |
| extra keys, wrong `id`, wrong `operator`, wrong `recovery`, non-object input, or any other schema failure | `invalid-shape` |

When the input matches `GRANTED_RESET_AUTHORIZATION` field-for-field
(including target order), `ok` is true and `value` is that record.

`classifyResetRecord` returns `null` for an unknown `(root, relativePath)`
pair. Known pairs are exactly this catalog (posix relative paths, no
leading slash):

| `root` | `relativePath` | disposition |
| --- | --- | --- |
| `home` | `access.json` | material |
| `home` | `admin-token` | material |
| `home` | `action-trust.json` | material |
| `home` | `porcelain` | disposable |
| `home` | `actions.json` | disposable |
| `home` | `board.json` | disposable |
| `home` | `layers.json` | disposable |
| `home` | `scope.json` | disposable |
| `home` | `notes.json` | disposable |
| `home` | `comments.json` | disposable |
| `home` | `reviewed.json` | disposable |
| `home` | `review-sets.json` | disposable |
| `home` | `feature-view.json` | disposable |
| `home` | `loop-evidence` | disposable |
| `userData` | `config.json` | material |
| `userData` | `projects-recents.json` | material |
| `repoCompanion` | `board.json` | material |
| `repoCompanion` | `actions.json` | material |
| `repoCompanion` | `notes.md` | material |
| `repoCompanion` | `layers.json` | material |
| `repoCompanion` | `scope.json` | material |
| `repoCompanion` | `.gitignore` | material |
| `repoCompanion` | `active-review` | material |
| `repoCompanion` | `reviews` | material |
| `repoCompanion` | `feature-view.json` | disposable |
| `repoCompanion` | `project-manifest.json` | disposable |
| `repoCompanion` | `.migrated-from-home` | disposable |

`isForbiddenLiveRoot` returns true when `absolutePath` is not
`path.isAbsolute`, or when `path.resolve(absolutePath)` equals or is
inside any of the compared live roots listed in ruling 6 (ignore
missing env values). It performs no I/O.

## Behavior to preserve

- Every real `~/.porcelain`, `~/.local/share/porcelain`,
  `~/.porcelain-dev`, `~/.local/share/porcelain-dev`, and checkout
  `.porcelain` tree is left untouched by this unit's code and tests.
- Decision 015: no silent delete of unreadable or obsolete files.
- DAT-001 `createStrictJsonDocument` behavior and path stay unchanged.
- PDT-001 manifest, PDT-002 notes/layers/dispositions, PDT-003 client
  adapters stay unchanged.
- `ensureProjectCompanion` remains defined in `migrate-home.ts`.
- Atomic tmp+rename, corruption backups, and intentional current-format
  defaults stay owned by their current modules.
- Promise discipline: no `void` of a promise; no no-op `.catch`. This
  module is synchronous.

## Legacy behavior to delete

Authorize no production deletion. Home/flat migrations, old aliases,
and old evidence readers remain on disk. This unit adds no export or
reset command to delete.

## Ordered implementation

1. Confirm HEAD is PDT-003 Landed (`80088e35` or later with the four
   Project Data client roots registered and no
   `plans/architecture-refactor/authorization/` directory), clean
   worktree, and catalog deps Landed. On mismatch, return this unit
   to review.
2. Write `plans/architecture-refactor/authorization/pdt-004-human-reset.md`
   with the Target ownership headings and values.
3. Add `reset-authorization.ts` with the exact types, constants, catalog,
   and three functions. Add the test file. Do not export from the
   feature index. Do not import the module from any production caller.
4. Run the focused tests, `pnpm lint`, and `git diff --check`. Run the
   deletion searches.
5. Flip this recipe and the catalog row to **Landed** in the same
   implementation commit; leave a clean worktree; write the README
   review packet; do not push; do not start PDT-005.

## Tests

Fixture strategy: `withTemporaryDirectory('porcelain-pdt-004-')` from
`apps/daemon/src/testing/temporary-directory.ts`. Create representative
empty files under that absolute root only. Literal synthetic paths such
as `/synthetic/home` are allowed as `pathTemplate` values in negative
parse cases. Do not read, write, or `stat` `porcelainHome()`,
`homedir()`-resolved porcelain directories, or any checkout
`.porcelain`.

| File | Must prove |
| --- | --- |
| `reset-authorization.test.ts` | `parseResetAuthorization(GRANTED_RESET_AUTHORIZATION)` is `{ ok: true, value: GRANTED_RESET_AUTHORIZATION }` |
| same | relative `pathTemplate` `'porcelain'` → `relative-path` |
| same | duplicate `kind` or duplicate `pathTemplate` → `duplicate-target` |
| same | target without `rootDisposition` → `missing-classification` |
| same | `approved: false` or omitted `approved` → `absent-approval` |
| same | `exportMachinery: true` → `export-machinery-present` |
| same | `agentRunnable: true` or `pnpmWired: true` → `agent-runnable` |
| same | home kind paired with the userData template → `unknown-target` |
| same | extra key on the record → `invalid-shape` |
| same | every catalog row's `classifyResetRecord` matches the table; `'no-such-file'` on each root returns `null` |
| same | a `withTemporaryDirectory` tree that writes one file per catalog row classifies each file; `isForbiddenLiveRoot(tempDir)` is false |
| same | `isForbiddenLiveRoot(porcelainHome())` is true; `isForbiddenLiveRoot(join(homedir(), '.local/share/porcelain'))` is true; `isForbiddenLiveRoot('relative')` is true |
| same | the module source (read via `import.meta.url`) contains no `rm(`, `rmdir`, `rmSync`, `unlink`, or `unlinkSync` and does not import `node:fs` / `node:fs/promises` |
| same | `GRANTED_RESET_AUTHORIZATION.exportMachinery` is false and `agentRunnable` is false |

Do not add E2E. Do not add a test that spawns a daemon, starts a
browser, or invokes `pnpm` to reset data.

## Validation and evidence

```text
pnpm --dir apps/desktop exec vitest run \
  ../daemon/src/project-data/reset-authorization.test.ts
pnpm lint
git diff --check
pnpm verify
```

Campaign lane may defer `pnpm verify` to the campaign boundary; focused
vitest, `pnpm lint`, and `git diff --check` always run.

Deletion searches (must match):

```text
rg -n "exportMachinery:\\s*true|createResetCommand|resetPorcelainHome|backupPorcelainHome|exportPorcelainHome" \
  apps/daemon/src/project-data plans/architecture-refactor/authorization
rg -n "\"reset:|\"export:|\"backup:|\"wipe:" package.json apps/*/package.json packages/*/package.json
rg -n "ensureProjectCompanion" apps/daemon/src --glob '!**/migrate-home.ts' --glob '!**/*.{test,spec}.ts'
rg -n "from '\\./reset-authorization'|from \\\"\\./reset-authorization\\\"" apps/daemon/src/features
rg -n "rm -rf ~/.porcelain|rm -rf ~/.local/share/porcelain" \
  apps/daemon/src/project-data plans/architecture-refactor/authorization
```

First: zero (no export machinery, no reset command symbols). Second:
zero new package.json scripts (existing unrelated `exports` keys in
package.json files are not a match for these patterns). Third: zero
production callers outside `migrate-home.ts` (unchanged from PDT-001).
Fourth: zero — the feature index and other feature modules do not
import the new file. Fifth: zero fenced live-path `rm -rf` in the new
files.

The README review packet must state starting commit, final commit,
changed-file groups, focused test counts, lint result, every deletion
search result, diff-check result, deferred or run `pnpm verify`, clean
worktree result, and confirmation that nothing was pushed and no later
recipe was started.

## Forbidden shortcuts

- Do not add export/backup/restore machinery or a restore-verification
  field.
- Do not add a runnable reset script, CLI verb, or pnpm command.
- Do not resolve `porcelainHome()` or `PORCELAIN_USER_DATA` as a write
  or delete target.
- Do not delete or rewrite `migrate-home.ts` / `migrate-active-review.ts`.
- Do not export the new module from `features/project-data`.
- Do not register a new architecture target root or mark the domain
  complete.
- Do not touch port 43117, the production home, or a real checkout
  companion.
- Do not start PDT-005 or LCH-001.
- Do not use `HUSKY=0`, `--no-verify`, or `PORCELAIN_SKIP_VERIFY`.
- Do not push.

## Completion criteria

- [x] `plans/architecture-refactor/authorization/pdt-004-human-reset.md`
      records the granted approval, three named targets, and the
      human-only manual procedure with no export machinery and no
      agent-runnable command.
- [x] `apps/daemon/src/project-data/reset-authorization.ts` exports the
      granted record, catalog, `parseResetAuthorization`,
      `classifyResetRecord`, and `isForbiddenLiveRoot` as specified.
- [x] Focused tests prove the granted record parses, each named refusal
      fires, the catalog matches, fixture roots stay isolated, and the
      module performs no filesystem mutation.
- [x] Deletion searches pass; migrations still exist; no package.json
      reset/export script was added.
- [x] Focused tests, `pnpm lint`, `git diff --check` (and `pnpm verify`
      outside campaign) pass.
- [x] One implementation commit lands only PDT-004, marks recipe and
      catalog **Landed**, leaves a clean worktree, provides the README
      review packet, and finishes without pushing or starting PDT-005.

## Handoff

PDT-005 may delete `apps/daemon/src/project/migrate-home.ts` and
`migrate-active-review.ts` after this authorization. It still must not
run the human reset. LCH-001 may rehearse clean-v1 seed/protocol on
isolated fixtures and must reference this manual procedure instead of
inventing a backup-verified reset command. PDT-006 remains the
completed-domain gate. The executor leaves a clean worktree, provides
the README review packet, and finishes without pushing. No later recipe
was started.
